#pragma once
#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <unordered_map>
using namespace std;

// Enumeration of the basic three types
enum class Type {INT = 10086, DOUBLE, STRING};

// Enumeration of condition operators
enum class Operator {EQ, NE, LT, LE, GT, GE};